<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface_background">

    <!-- 1. 顶部导航栏 -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surface_card"
        app:layout_constraintTop_toTopOf="parent"
        app:menu="@menu/menu_font"
        app:navigationIcon="@drawable/ic_arrow_left"
        app:title="字号调整"
        app:titleCentered="true" />

    <!-- 2. 预览区域 (保持不变) -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="@dimen/spacing_m"
        app:layout_constraintBottom_toTopOf="@id/card_control"
        app:layout_constraintTop_toBottomOf="@id/toolbar">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- 模拟 用户 消息 -->
            <include 
                android:id="@+id/preview_msg_user"
                layout="@layout/item_msg_user" />

            <Space
                android:layout_width="match_parent"
                android:layout_height="@dimen/spacing_l" />
            <!-- 模拟 AI 消息 -->
            <include 
                android:id="@+id/preview_msg_ai"
                layout="@layout/item_msg_ai" />


        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

    <!-- 3. 底部控制面板 -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_control"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="@color/surface_card"
        app:cardElevation="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.TopRounded">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="@dimen/spacing_l"
            android:paddingTop="32dp"
            android:paddingBottom="32dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="A"
                    android:textColor="@color/text_primary"
                    android:textSize="14sp"
                    android:textStyle="bold" />

                <!--
                    Slider 样式深度定制：
                    1. trackColorActive/Inactive 都设为 surface_deep_think (浅灰)，消除进度感；
                    2. thumbColor 设为白色，加阴影和描边；
                    3. labelBehavior="floating" 显示上方气泡。
                -->
                <com.google.android.material.slider.Slider
                    android:id="@+id/slider_font_size"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginHorizontal="12dp"
                    android:stepSize="1.0"
                    android:valueFrom="0.0"
                    android:valueTo="3.0"
                    android:value="1.0"
                    app:labelBehavior="floating"
                    app:tickVisible="true"
                    app:trackColorInactive="#E0E0E0"
                    app:trackColorActive="#E0E0E0"
                    app:trackHeight="4dp"
                    app:thumbColor="@color/white"
                    app:thumbRadius="10dp"
                    app:thumbElevation="4dp"
                    app:thumbStrokeColor="#CCCCCC"
                    app:thumbStrokeWidth="1dp"
                    app:tickColorActive="@color/text_tertiary"
                    app:tickColorInactive="@color/text_tertiary"
                    app:haloColor="#1A000000" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="A"
                    android:textColor="@color/text_primary"
                    android:textSize="24sp"
                    android:textStyle="bold" />
            </LinearLayout>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

</androidx.constraintlayout.widget.ConstraintLayout>
